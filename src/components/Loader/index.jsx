import React from "react";

import { LoaderWrapper, Spinner } from "./styled";

const Loader = () => {
  return (
    <LoaderWrapper>
      <Spinner />
    </LoaderWrapper>
  );
};

export default Loader;
